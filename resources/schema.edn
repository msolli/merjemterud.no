[;; Extra page attributes used by content/index.md (the front page).
 ;; Powerpack stores every page in Datomic, so every mapdown key must be
 ;; declared here (it already ships :page/uri, :page/title and :page/body).

 ;; Hero
 {:db/ident       :hero/kicker
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}
 {:db/ident       :hero/title
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}
 {:db/ident       :hero/tagline
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}
 {:db/ident       :hero/lead
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}

 ;; Intro / program
 {:db/ident       :intro/body
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}

 ;; Bursdagspresang / tickets
 {:db/ident       :gift/title
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}
 {:db/ident       :gift/body
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}

 ;; Call to action
 {:db/ident       :cta/label
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}
 {:db/ident       :cta/url
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}

 ;; Front-page program teaser
 {:db/ident       :teaser/body
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}

 ;; Hvem kan melde seg på?
 {:db/ident       :who/title
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}
 {:db/ident       :who/body
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}

 ;; Program page (/program)
 {:db/ident       :program/note
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}
 {:db/ident       :program/fredag
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}
 {:db/ident       :program/lordag
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}
 {:db/ident       :program/aktiviteter
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}
 {:db/ident       :program/foredrag
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}

 ;; Praktisk page (/praktisk)
 {:db/ident       :sted/body
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}
 {:db/ident       :overnatting/body
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}
 {:db/ident       :mat/body
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}
 {:db/ident       :transport/body
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one}]
